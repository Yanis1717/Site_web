{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

<style>
    /* 1. bleu personnalisé pour le container du bas */
    .bg-bleu-perso {
        background-color: #5e84c0 ;
        color: #ffffff ;
    }

    /* 2. STYLE DE LA CARTE PROFIL */
    .profile-card {
        background: #ffffff;
        border: none;
        border-radius: 45px;
        overflow: hidden;
        transition: transform 0.3s ease;
    }
    
    .profile-card:hover {
        transform: translateY(-5px); /* Légère levée au survol */
    }

    /* Le bandeau de couleur en haut de la carte */
    .profile-header {
        height: 120px;
        background: linear-gradient(135deg, #5e84c0, #8bb1f3); /* Dégradé bleu */
    }

    /* Le conteneur de la photo pour la positionner à cheval sur le bandeau */
    .profile-img-container {
        margin-top: -60px; 
        display: flex;
        justify-content: center;
    }

    /* La photo de profil ronde */
    .profile-img {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        border: 4px solid white; /* Bordure blanche pour la détacher du fond */
        object-fit: cover;
        box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    }
</style>

<div class="container text-center mb-5 pt-4">
    <h1 class="display-4 fw-bold text-dark" style="font-size: 4rem;">Yanis LAÏD</h1>
    <h2 class="h3 text-muted mt-3">Futur administrateur en Réseaux & Télécoms</h2>
    <p class="lead mt-3">À la recherche d'une alternance pour <span class="badge bg-bleu-perso">2026-2027</span></p>
</div>

<div class="container my-4 d-flex justify-content-center">
    
    <div class="card profile-card shadow text-center" style="width: 350px;">
        <div class="profile-header"></div>
        
        <div class="profile-img-container">
            <img src="{{ asset('images/moi.jpeg') }}" alt="Yanis LAÏD" class="profile-img">
        </div>
        
        <div class="card-body">
            <h3 class="fw-bold mt-2">Yanis LAÏD</h3>
            <p class="text-muted mb-3">Étudiant en BUT R&T</p>
            
            <hr class="w-50 mx-auto my-3">
            
            <p class="card-text text-secondary">
                <i class="fas fa-university me-2"></i>IUT de Roanne<br>
                <i class="fas fa-map-marker-alt me-2"></i>Roanne, France
            </p>
            
            <div class="mt-4 mb-2">
                <p> Me contacter par mail :</p>
                <a href="mailto:yanislaid8@gmail.com" class="btn btn-outline-primary btn-sm rounded mx-1"><i class="fas fa-envelope"></i></a>
            </div>
        </div>
    </div>

</div>

<div class="container my-5">
    <div class="p-5 bg-bleu-perso border rounded-3 shadow-sm">
        <div class="row align-items-center">
            <div class="col-lg-8">
                <h2 class="fw-bold text-white mb-3">Pour en savoir plus</h2>
                <p class="lead mb-0 text-white">
                    Découvrez mon univers en images. J'y partage mes <strong>loisirs</strong>, 
                    mes <strong>passions</strong>, ainsi que les <strong>projets</strong> qui me tiennent à cœur.
                </p>
            </div>
            <div class="col-lg-4 text-center mt-4 mt-lg-0">
                <button class="btn btn-light btn-lg px-4" onclick="location.href='{{ path('app_loisirs') }}'">
                    <i class="fas fa-file-alt me-2"></i>Explorer ma galerie
                </button>
            </div>
        </div>
    </div>
</div>

{% endblock %}
